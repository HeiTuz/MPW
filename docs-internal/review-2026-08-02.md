# HeiTuz 6-repository code review — 2026-08-02

Scope: local trees under `~/HeiTuz`, ownership/deployment wiring, and `~/.hermes/scripts/prompt_writing_doctrine_check.py`. No remote push. Existing `~/HeiTuz/ImgGen2/.codegraph/.gitignore` deletion was pre-existing and was not touched.

## Verification evidence

- MPW common suite with all four adapter roots injected: `python3 -m unittest discover scripts -p 'test_*.py'` — `Ran 137 tests ... OK`.
- MPW prompt fixtures: `node scripts/check_prompt.mjs --test` — `38/38 fixtures green`.
- MPW lint: `python3 scripts/lint.py` — `OK — 24 files, 10 measured labels, all checks passed`.
- ImgGen2 common suite: `python3 -m unittest discover scripts -p 'test_*.py'` — `Ran 172 tests ... OK`.
- ImgGen2 runtime suite: `node scripts/run_python_tests.mjs` — completed with all listed tests `ok`.
- image-reference-gardener: common unittest completed `OK`; `python3 scripts/lint.py` exit 0.
- design-reference-gardener: common unittest completed `OK`; `python3 scripts/lint.py` exit 0.
- prompt-knowledge-gardener: common unittest completed `OK`; `python3 scripts/lint.py` exit 0.
- higgsfield-prompt-bridge: common unittest completed `OK`.
- Initial MPW common run without adapter roots failed in `setUpClass` as designed; it was rerun with all four `~/HeiTuz` roots and passed. This is recorded as a gate finding, not green evidence.

## Findings

[MPW] AGENTS.md:3-10 · The repository identifies itself as the canonical editable tree and documents only Claude Code/Hermes member-link consumers; it does not define Codex as the canonical editor or distinguish Codex's copied install payload from the source tree · 배선 결함 · 수리하지 않음(ownership transfer is the stage-2 change and must be applied consistently across all six repository policies and host guidance) · `git -C ~/HeiTuz/MPW status --short --branch` was clean before this report; the AGENTS text is the source evidence.

[MPW] AGENTS.md:43-59 · The required doctrine check is described as a symlink-only consumer check, while the observed Codex MPW install is a real copied tree (`~/.codex/skills/MPW`, 9 members, 0 symlinks) and doctrine stdout reports that detached copy · 배선 결함 · 수리하지 않음(doctor rule and host layout must change together in stage 2) · `python3 ~/.hermes/scripts/prompt_writing_doctrine_check.py` stdout reported `MPW: codex install path is no longer a link into /Users/eusin/HeiTuz`.

[ImgGen2] AGENTS.md · repository has no local AGENTS.md ownership policy, while README.md:62-69 and agents/README.md:3-13 describe canonical source plus per-host copied overlays without naming Codex's edit authority · 배선 결함 · 수리하지 않음(stage 2 must add the policy without changing the skill payload) · `test -f ~/HeiTuz/ImgGen2/AGENTS.md` returned false; `agents/README.md` is the source evidence.

[ImgGen2] agents/README.md:7-13 · The install model explicitly creates a copied payload (`canonical allowlisted tree + agents/<host>/ overlay`) but host guidance and doctrine currently treat deployment paths as symlink consumers; this leaves source/install drift possible · 배선 결함 · 수리하지 않음(stage 2 must define copy parity and re-install authority; do not weaken the check) · source text plus observed `~/.codex/skills/ImgGen2` real directory with 9 non-symlink members.

[image-reference-gardener] AGENTS.md:3-11 · The repository states it is the canonical skill and forbids direct edits to other skills, but does not state Codex as the canonical editor or Claude Code/Hermes as consumers · 배선 결함 · 수리하지 않음(stage 2 policy update) · source text.

[design-reference-gardener] AGENTS.md:3-15 · The repository states canonical ownership but has no Codex editor/consumer boundary and no copied-install parity rule · 배선 결함 · 수리하지 않음(stage 2 policy update) · source text.

[prompt-knowledge-gardener] AGENTS.md:3-11 · The repository states canonical ownership but has no Codex editor/consumer boundary and no copied-install parity rule · 배선 결함 · 수리하지 않음(stage 2 policy update) · source text.

[higgsfield-prompt-bridge] AGENTS.md:3-5 · The repository describes Claude Code/Hermes symlink consumption but omits Codex and does not define the editor boundary · 배선 결함 · 수리하지 않음(stage 2 policy update) · source text.

[host] ~/CLAUDE.md:63-70 · Host boundary says the canonical source is a repository and Claude/Hermes consume symlinks, but separately documents ImgGen2 copied deployment and does not define Codex as the editing authority or state action permissions · 배선 결함 · 수리하지 않음(stage 2 host-boundary update) · source text.

[host] ~/AGENTS.md · Orchestration policy routes work to Codex but contains no HeiTuz ownership definition or action matrix · 배선 결함 · 수리하지 않음(stage 2 host-boundary update) · source text.

[doctrine] ~/.hermes/scripts/prompt_writing_doctrine_check.py:384-456 · checker assumes every present deployment path must link into `~/HeiTuz`, but the approved install model includes copied host overlays; current stdout is non-empty for the Codex MPW copy · 배선 결함 · 수리하지 않음(stage 2 checker update must enforce the new definition rather than silence the finding) · direct checker run above.

## Stage-2 handoff payload (ownership-wiring findings only)

Review report commit: **63f934d6d06751b25913d24d0f7d63fbc3056bae**

[MPW] AGENTS.md:3-10 · The repository identifies itself as the canonical editable tree and documents only Claude Code/Hermes member-link consumers; it does not define Codex as the canonical editor or distinguish Codex's copied install payload from the source tree · 이 결함은 Codex가 `~/HeiTuz/MPW`를 정본 편집 표면으로 소유한다는 운영 정의와, 설치본은 소비용 산출물이라는 경계를 문서화하도록 바꾼다.

[MPW] AGENTS.md:43-59 · The required doctrine check is described as a symlink-only consumer check, while the observed Codex MPW install is a real copied tree and doctrine stdout reports that detached copy · 이 결함은 복사 설치본의 정본 패리티/재설치 규칙을 집행하도록 doctrine 검사를 바꾼다.

[ImgGen2] AGENTS.md · repository has no local ownership policy while README.md and agents/README.md describe copied overlays · 이 결함은 ImgGen2에도 Codex 정본 편집·호스트 소비·재설치 경계를 명시하게 한다.

[ImgGen2] agents/README.md:7-13 · copied overlay install model is not reconciled with symlink-only doctrine · 이 결함은 Codex 설치본을 편집 장소가 아닌 검증 가능한 소비 산출물로 정의하게 한다.

[image-reference-gardener] AGENTS.md:3-11 · canonical policy omits Codex editor and Claude/Hermes consumer roles · 이 결함은 gardener 정본 편집 권한과 소비자 경계를 명시하게 한다.

[design-reference-gardener] AGENTS.md:3-15 · canonical policy omits Codex editor and copied-install parity · 이 결함은 design gardener의 동일한 소유권·패리티 계약을 명시하게 한다.

[prompt-knowledge-gardener] AGENTS.md:3-11 · canonical policy omits Codex editor and copied-install parity · 이 결함은 prompt knowledge gardener의 동일한 소유권·패리티 계약을 명시하게 한다.

[higgsfield-prompt-bridge] AGENTS.md:3-5 · policy names Claude/Hermes consumers but omits Codex editor boundary · 이 결함은 bridge 정본 편집 표면과 소비자 역할을 명시하게 한다.

[host] ~/CLAUDE.md:63-70 · host boundary omits Codex editor authority and action permissions · 이 결함은 Claude Code/Hermes의 금지 행동과 Codex의 허용 행동을 문서화하게 한다.

[host] ~/AGENTS.md · routing policy omits HeiTuz ownership/action matrix · 이 결함은 Codex 편집 권한과 Claude/Hermes 소비자 역할을 오케스트레이션 경계에 반영하게 한다.

[doctrine] ~/.hermes/scripts/prompt_writing_doctrine_check.py:384-456 · symlink-only enforcement rejects the observed copied Codex install · 이 결함은 checker가 symlink는 symlink대로, copied overlay는 canonical-source parity와 installation provenance로 실제 새 정의를 집행하도록 바꾼다.

## Stage-1 disposition

The initial review pass found no product logic, schema, contract, or cross-contract mirror change that could be safely applied without the approval boundary. Its required verification rerun was green. A later independent gardener review (agent artifact `agent://2-ReviewGardeners`) found additional release-blocking defects, so the stage-1 report is amended below and stage 1 is not complete until those findings receive an explicit repair or reasoned disposition.
## Review addendum from independent gardener lane

[image-reference-gardener] scripts/image_reference_garden.py:557-614 · normal manifest mutations bypass the existing lock/atomic writer, risking hash/status clobbering under concurrent CLI calls · 수리권장 · 수리하지 않음(현재 목표의 소유권 배선 전환에서 파일 트랜잭션 구조를 변경하면 별도 결함수리 범위가 되며, 직접 수정 없이 후속 수리로 넘김) · agent review evidence cites `references/gardener.md:88-90` and existing `manifest_lock`/`save_manifest_atomic`.

[image-reference-gardener] scripts/image_reference_garden.py:471-475,704-826 · pending transitions rewrite JSON without a per-scope transaction, violating the one-slot/prior_status contract under concurrent events · 수리권장 · 수리하지 않음(동일 사유: 별도 concurrency repair scope) · agent review evidence.

[design-reference-gardener] scripts/gardener_loop.py:253-266 · `compiler_owner: "HeiTuzMPW"` disagrees with canonical MPW wire owner `MPW` · 배포차단 · 수리함(ownership handoff now emits `MPW`; test updated in design commit `90fb0c40be1ce2641c81f9c7012874b41ac9be7c`) · agent review evidence cites canonical `~/HeiTuz/MPW/contracts/v1/mpw-recompile-request.schema.json:7-10`.

[design-reference-gardener] scripts/design_reference_garden.py:661-675 · `--url` encodes a full URL into `image_bytes_hex`, allowing URL credentials/query material to be persisted despite hash-only policy · 배포차단 · 수리하지 않음(privacy/representation contract change requires explicit approval and a dedicated test design; not changed during ownership transfer) · agent review evidence.

[Both gardener repos] migration residue in `/Users/eusin/src/.gjc/context/gardener-master-prompt-loop-20260711T115142Z.md` and divergent `/Users/eusin/src/MPW-release/contracts/` remains unquarantined · 수리하지 않음(`~/src/MPW-release` is an explicit non-goal; no mutation allowed) · agent review evidence.

## Revised stage-1 disposition

Stage 1 remains **blocked** by the concurrency/privacy findings that were explicitly not changed in this ownership-transfer scope. The design compiler-owner defect is repaired and verified by `python3 -m unittest scripts.test_design_reference_garden` (`Ran 23 tests ... OK`). The report is not a clean green review; the independent lane's broader commands were intentionally skipped per its read-only assignment, while the required repo verification evidence above remains current. Stage 2 ownership edits remain retained, but the aggregate cannot be declared complete while those recorded review blockers remain unresolved.
