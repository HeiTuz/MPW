# from-image 레인 — 이미지가 입력으로 들어올 때

입력의 역할을 먼저 구분한다. **원본 편집**은 변경·보존 계약으로, **취향 레퍼런스**와 **생성물 재컴파일**은 필요한 관찰 레코드와 권한 배선으로 처리한다.

**이 파일이 정본인 것:** 관찰 레코드의 축 정의(§3), 생성물 재컴파일 함정(§4), 참조의 유지·변경 축과 요청별 비교 기준(§5), 관찰 → 타깃별 컴파일의 `feeds` 계약(§6).

**이 파일이 정본이 아닌 것 — 참조만 한다:**

| 소관 | 정본 |
|---|---|
| 길이·비율·해상도·품질·파라미터 | [surfaces.md](surfaces.md) |
| 축별 단일 권한·엣지 판정·체인 | [../prompt-graph.md](../prompt-graph.md) |
| 목적축 → 모델 후보 | [model-routing.md](model-routing.md) |
| 네이티브 제약 / MPW 컴파일 티어 | [surface-contracts.md](surface-contracts.md) §3.1·§4 / [compiler.md](compiler.md) §2 |
| 레인 게이트 카드·이미지 슬롯 기본값 | [lanes.md](lanes.md) |
| 의상 관찰 스키마·파생물 | 설치가 공급한다. 없으면 §3 관찰 레코드로 직접 작성한다 |
| 비전 반환형 타입 안전 | 설치가 공급한다. 없으면 §2 절차로 반환형을 판정한다 |

§5는 요청한 유사점과 차이점을 정리하는 작성 도구다. 축 개수만으로 품질·권리·안전을 판정하지 않는다.

---

## 1. 입력 역할과 진입점

사용자가 지정한 역할이 먼저다. 편집 대상, 제품·인물 정체성 참조, 스타일 참조, 시작·끝 프레임을 구분하고 각 입력이 정하는 축만 사용한다. **원본 편집은 [lanes.md](lanes.md) §편집·제품 배치 압축 또는 §배경 합성 레인으로 바로 간다.** 변경점·보존 조건·출력 수를 유지하며 별도 취향 변주를 끼워 넣지 않는다. 제품·인물 참조도 명시된 보존 축을 임의로 새 디자인·새 얼굴로 바꾸지 않는다.

다음 두 진입점은 이미지에서 취향을 추출하거나 생성 의도를 복원하는 요청에 적용한다.

| | **취향 레퍼런스** | **생성물 재컴파일** |
|---|---|---|
| 입력 | 무드보드·화보·스크랩 | 내가 만든 이미지 |
| 목적 | 요청한 유사점·차이점을 반영한 결과 | 재현·반복·다른 플랫폼으로 이식 |
| 레퍼런스가 갖는 권한 | 사용자가 지정한 스타일·내용 축. 가져올 범위를 임의로 축소하지 않음 | 재현할 화면·의도 중 요청한 축. 결함 여부는 의도와 대조 |
| 유지·변경 범위 | 요청한 유사점과 새로 만들 부분 (§5) | 재현·수정·이식 목적에 맞는 범위 (§5) |
| 원래 프롬프트 존재 여부 | 묻지 않는다 | 대화에 있으면 사용. 의도 복원의 핵심이 불명확할 때만 질문 (§4.5) |

**공유하는 것:** §2 비전 경계, §3 관찰 레코드, §6 컴파일 타깃, §7 게이트.

캐릭터시트 요청의 양식 선택은 [ghost-character-reference-sheet.md](ghost-character-reference-sheet.md) §1을 따른다. 3패널 양식은 §3 관찰 레코드 위에 축별 기준·패널 판정만 얹는다. 그 양식의 CENTER 후면 추정은 §3.2 "보이지 않는 것을 발명하지 않는다"의 명시 예외이며 INFERRED로 표시한다.

### 1.1 진입점 판정 — 한 줄 질문

먼저 편집·보존 요청인지 판정한다. 이에 해당하지 않고 생성 의도 복원이 목적이면 생성물 재컴파일, 같은 감도의 새 작품이 목적이면 취향 레퍼런스다.

역할이 불명확하고 선택에 따라 얼굴·상품·카피 등 보존 결과가 달라질 때만 해당 역할을 확인한다. 출처가 불명이라는 이유만으로 편집 대상을 취향 레퍼런스로 낮추지 않는다.

### 1.2 혼합 입력 — 레퍼런스가 여럿일 때

레퍼런스가 여럿이면 **각 입력이 정하는 축을 배정한다.** 같은 인물의 정면·측면처럼 보완되는 자료는 하나의 권한 묶음으로 쓸 수 있다. 서로 다른 의상·얼굴·색감처럼 충돌하는 값만 우선순위를 정한다([prompt-graph.md](../prompt-graph.md) §7). 장수만으로 충돌이나 재현 실패를 단정하지 않는다.

- 배선 예: `ref-01`(내 생성 인물컷) = I9 얼굴 기하 + I6 의상 / `ref-02`(외부 무드보드) = S1~S4 감도.
- 충돌하면 산출 첫 문장에 **우선순위 1줄**을 박는다. 관련 함정 정본: [image-reference-editing-prompt-pitfalls.md](../image-reference-editing-prompt-pitfalls.md), [image-reference-wardrobe-preservation-pitfall.md](../image-reference-wardrobe-preservation-pitfall.md), [portrait-reference-moderation-triage.md](portrait-reference-moderation-triage.md).
- 어떤 축의 권한자도 아닌 레퍼런스는 **빼거나 축을 배정한다.** 넣어놓고 안 쓰는 것은 고아 EVIDENCE다.

### 1.3 이어서 편집할 때

직전 결과 이미지와 필요한 대화 상태가 실제로 전달되면 이번 변경점과 핵심 보존 조건만 쓴다. 새 호출에 이전 이미지·상태가 없으면 먼저 입력을 연결하고, `아까처럼`만으로 편집을 맡기지 않는다. 입력별 역할은 도구가 지원하는 참조 번호·이름으로 짧게 연결하며, 모델마다 다른 인덱스 문법을 임의로 통일하지 않는다. 독립 원본의 일괄 편집은 직전 결과를 다음 상품의 원본으로 이어 쓰지 않는다.

Midjourney 일반 Image Prompt는 완성 장면을 묘사하며, 기존 이미지의 변경 지시는 [../midjourney-identity.md](../midjourney-identity.md)의 Edit Model 경로로 보낸다.

---

## 2. 비전 런타임 중립 — 반환형이 달라도 레코드는 하나

런타임마다 반환형이 다르다. **두 경로를 모두 지원하고, 어느 쪽이든 도착점은 §3 관찰 레코드다.**

| 경로 | 상황 | 처리 |
|---|---|---|
| **A. 도구 반환** | 비전 도구가 텍스트·객체·리스트를 반환 | 원시 payload를 문자열 처리기에 넘기지 않는다. 값이 텍스트인지 확인한 뒤에만 정규식·직렬화를 건다 |
| **B. 네이티브 비전** | 이미지가 컨텍스트에 붙고 에이전트가 직접 읽음. 도구는 확인 응답만 반환하거나 아예 없음 | 에이전트가 픽셀을 읽어 레코드를 **직접 작성한다.** 도구 응답 부재를 실패로 처리하지 않는다 |

세부 계약은 재정의하지 않는다. 설치가 타입 안전 계약을 공급하면 그것을 따르고, 없으면 이 절의 두 경로와 네 규칙으로 직접 판정한다.

1. **내부 라벨은 `ref-01`, `ref-02` …** 첨부 순서를 보존한다. 산출에는 해석되지 않는 내부 라벨·로컬 경로 대신 실제 입력에 연결된 참조 표현만 쓴다(§6.4).
2. **관찰이 필요한 축에는 값 또는 상태를 남긴다.** 내부 기록이며 모든 축을 채울 의무는 없다. 원본 편집은 역할·변경점·보존 조건으로 충분하면 상세 관찰표를 만들지 않는다.
3. **단일 이미지는 리스트를 만들지 않는다.** 1항목 리스트를 만들었다가 문자열로 강제 변환하는 경로가 실패 원인이다.
4. **타입 실패 후 재시도 금지.** 같은 호출을 반복해 같은 원시값을 같은 포매터에 다시 넣지 않는다. 이미 로드된 이미지를 그대로 두고 수동으로 레코드를 써서 같은 턴에 진행한다. 예외 메시지를 산출 프롬프트 자리에 대신 넣지 않는다.

---

## 3. 관찰 레코드 — 플랫폼 중립

### 3.1 한 줄의 형식

```text
<ref 라벨> | <축> | <상태> <confidence> | <값 — 결과 어휘 + 수치> | 근거: <픽셀 단서>
```

예:

```text
ref-01 | S1 조명 | observed 0.8 | 좌 45° 창광, 소프트, 키:그림자 약 2:1, 그림자 가장자리 부드러움 | 근거: 좌뺨 하이라이트 + 우측 코그림자 경계 폭
ref-01 | I6 의상 아이템 | observed 0.6 | 오버사이즈 더블 브레스트, 노치트 라펠, 무릎 아래 길이 | 근거: 라펠 절개선·단추 2열
ref-01 | I8 렌더 텍스트 | unreadable 0.9 | 좌상단에 4~6글자 상당 문자열, 글리프 불성립 | 근거: 자획이 닫히지 않음
```

### 3.2 상태 5종 — 승격 금지

| 상태 | 뜻 | 컴파일에 실리는가 |
|---|---|---|
| `observed` | 픽셀에서 직접 읽음 | 실린다 |
| `inferred` | 다른 관찰에서 추론 | 실린다. 단 confidence를 함께 남긴다 |
| `unreadable` | 있는 건 보이나 판독 불가 | 실리지 않는다. 값을 만들지 않는다 |
| `not_visible` | 프레임·가림 때문에 안 보임 | 실리지 않는다 |
| `not_applicable` | 그 축이 이 이미지에 없음 | 실리지 않는다 |

**보이지 않는 것을 발명하지 않는다.** 뒤판·안감·잠금장치·브랜드·소재 혼용률·정확 치수는 안 보이면 안 보이는 것이다. `unreadable`/`not_visible`을 `observed`로 올리는 순간 그 값은 근거 없는 지시가 되고, 모델은 그것을 사실로 렌더한다.

### 3.3 축 목록 — 필요한 관찰만 고른다

아래는 관찰을 구분하기 위한 어휘다. 요청에 필요한 축만 기록하며 축 분류 자체가 품질·복제 판정은 아니다.

**빛·색·촬영 조건**

| 축 | 기록할 성분 |
|---|---|
| **S1 조명** | 광원 방향(각도), soft/hard, 키:그림자 비율, 그림자 가장자리 성질, rim 유무, 켈빈 |
| **S2 카메라** | 카메라-피사체 거리, 얼굴·평면 압축 정도, 심도 깊이, 배경 분리 정도, 샷 사이즈 대역 |
| **S3 그레이딩** | 채도 대역, 콘트라스트 커브, 색온도 편향, shadow/highlight 롤오프, 그레인 양, 노출 편향 |
| **S4 프레임 정책** | 네거티브 스페이스 비율(%), 정면성 vs 스냅, 대칭 선호, 헤드룸, 피사체 크기 비중 |

**장면의 대상·배치·문구**

| 축 | 기록할 성분 |
|---|---|
| **I1 포즈** | 팔·다리 관절 각도 조합, 머리 기울기, 무게중심 |
| **I2 시선·표정** | 시선 방향 + 표정의 특정 조합 |
| **I3 손** | 손 위치·제스처·소지물 |
| **I4 소품** | 소품 종류 + 프레임 내 배치 |
| **I5 공간 정체** | 그 벽·그 건축 요소·랜드마크·실재 매장 |
| **I6 의상 아이템 정체** | 그 실루엣의 그 아이템 |
| **I7 크롭·배치** | 피사체 프레임 내 좌표, 눈이 오는 분할선 |
| **I8 렌더 텍스트** | 카피 문자열, 타이포 배치 (기본 상태는 §4.3) |
| **I9 얼굴 기하** | 페이스 랜드마크 |

### 3.4 결과 어휘 + 수치 — 형용사는 레코드가 아니다

감도 축은 광원의 방향·그림자·색 관계·프레이밍처럼 관찰 가능한 결과로 적는다. 실측하지 않은 거리·켈빈·비율을 채우지 않는다. 필요한 구체성은 [compiler.md](compiler.md) 철칙 3·4·5·10을 따른다.

### 3.5 의상이 주제면 위임한다 — 여기서 스키마를 다시 쓰지 않는다

| 상황 | 처리 |
|---|---|
| 의상이 결과의 하드락(그 아이템을 정확히 재현해야 함) | 설치가 공급한 의상 분석 계약으로 **위임**. 없으면 §3 관찰 레코드로 직접 작성하고, 관찰 불가 값은 발명하지 않는다 |
| 의상이 감도 성분만(실루엣 계열·소재 질감 정책) | I6 한 줄로 충분. 위임하지 않는다 |

위임 경로에서 온 값을 이 파일의 축 표로 옮겨 적지 않는다. 두 곳에 같은 값이 있으면 드리프트가 시작된다.

---

## 4. 생성물 재컴파일 — 이 진입점에만 있는 함정

취향 레퍼런스에는 없고 **내가 만든 이미지를 다시 프롬프트로 되돌릴 때만** 생기는 결함 네 종이다.

### 4.1 값마다 3태그 — `intent` / `signature` / `artifact`

생성물 재컴파일에서 의도와 결함을 구별할 필요가 있을 때만 §3.1 레코드에 태그를 붙인다.

| 태그 | 판정 질문 | 컴파일 |
|---|---|---|
| `intent` | 내가 프롬프트로 지시했던 것인가 | 그대로 실린다 |
| `signature` | 대조 근거가 있는 모델의 반복적 특성인가 | §4.3 분기 |
| `artifact` | 요청 의도에 어긋나는 생성 결함인가 | 의도적 표현이 아니면 제외한다 |

사용자가 의도한 초현실·비대칭·왜곡과 비의도적 결함을 구분한다. 픽셀만으로 생성 의도나 오류 원인을 확정하지 않는다. 모델명만으로 `signature`를 확정하지 않는다. 비교 근거가 없으면 관찰된 특성으로만 기록한다.

### 4.2 AI 아티팩트 — 충실한 관찰이 다음 생성에서 재현된다

관찰이 정직할수록 결함이 정확히 프롬프트에 실리고, 다음 생성이 그걸 그대로 재현한다. **이게 재컴파일 최대 실패 모드다.**

판정 가능한 `artifact` 목록:

- 손가락 수·관절 방향이 성립하지 않음
- 글리프가 닫히지 않는 문자, 유령 글자, 중복 문자열
- 이어지지 않는 봉제선·스트랩·지퍼, 사라지는 소매
- 비대칭 액세서리(귀걸이 한쪽, 안경 다리 한쪽)
- 반복 패턴의 어긋난 이음매, 배경 격자·타일의 끊김
- 접지 그림자 없는 발, 그림자 방향이 광원과 어긋남
- 얼굴·신체 일부만 다른 해상도로 뭉개짐

규칙 두 줄:

1. **레코드에는 남기고 프롬프트에서는 뺀다.** 남겨야 "왜 안 실었는지"가 추적된다. 의도적으로 요청된 왜곡·비대칭·초현실 표현은 결함으로 지워버리지 않는다.
2. **아티팩트를 부정문으로 배제하지도 않는다.** `손가락 6개 없음`은 부정문이고, 애초에 손가락 개수는 프롬프트 축이 아니다. 침묵이 정답이다. 손·글자 결함의 일반 예방은 [realism.md](realism.md)와 [compiler.md](compiler.md) §2 소관이다.

### 4.3 모델 서명 — 재서술하면 이중 적용된다

결과물에는 이미 그 모델의 서명이 얹혀 있다. 그걸 관찰해서 프롬프트에 다시 쓰면 **다음 생성에서 두 번 적용된다** — 보케가 더 무너지고, 피부가 더 미끄럽고, 색온도가 더 치우친다.

판정 질문: **"이 성질이 그 모델의 무프롬프트 산출에도 나타나는가?"** 예면 `signature`.

전형적 서명: 과한 보케·배경 붕괴, 특유의 피부 광택·과매끈, 기본 색온도 편향, 중앙 정렬 구도 편향, 과채도, 얼굴 대칭화, 기본 시네마틱 그레이드.

| 다음 생성 타깃 | `signature` 처리 |
|---|---|
| **같은 모델로 다시 돌린다** | 프롬프트에 **쓰지 않는다.** 자동으로 온다. 억제하려면 반대 방향 결과 어휘를 쓴다(예: 배경 분리를 줄이려면 심도 깊이를 결과로 지정) |
| **다른 모델로 이식한다** | 결과 어휘로 **명시적으로 재서술한다.** 이때만 싣는다 |

서명인지 의도인지 확신이 없으면 이식 쪽에만 싣는다(보수적). 같은 모델 재실행에서 빠진 것은 어차피 다시 나오지만, 이식에서 빠진 것은 사라진다.

### 4.4 렌더 텍스트 — 기본 상태는 `unreadable`

생성 이미지에서 깨진 글자를 전사하면 그 문자열이 다음 생성의 카피가 될 수 있다. 읽히는 문자열과 의도한 정확 카피를 구분한다.

- 읽히는 문자열은 `observed`, 판독할 수 없는 문자열은 `unreadable`로 기록한다. 사용자가 제공한 정확 카피가 있으면 그것이 권한자다.
- 원본 글자 보존 요청은 그대로 유지한다. 새로 렌더할 정확 문자열이 필요한데 판독할 수 없을 때만 확인하며, 확인 전 임의 문구나 `문자 없는 클린 마감`으로 대체하지 않는다.
- **카피의 권한자는 사용자이지 관찰이 아니다.** 카피는 롤 라벨 블록 1곳에만 둔다([prompt-graph.md](../prompt-graph.md) §4).
- 타이포 배치(레이아웃 좌표)는 카피와 별개 축이다. 배치는 `observed`일 수 있고 문자열은 `unreadable`일 수 있다.

### 4.5 원래 프롬프트가 있으면 그게 권한자다

원래 프롬프트가 대화에 있으면 사용한다. 없더라도 요청과 이미지로 작성할 수 있으면 진행하며, 보이지 않는 생성 의도가 결과를 바꿀 때만 질문한다.

| 있음 | 없음 |
|---|---|
| 원래 프롬프트·모델 id·파라미터·seed가 **권한자**. 관찰은 델타 확인용으로만 쓴다 | 관찰이 권한자. 관찰로 복원할 수 없는 축(seed, 내부 샘플링)은 **재현 대상에서 뺀다** |

- **완전 동일 재현을 약속하지 않는다.** seed가 없으면 같은 프롬프트도 같은 이미지를 주지 않는다.
- **다른 플랫폼으로 이식할 때는 호환되지 않는 문법·설정만 바꾼다.** 유효한 장면·카피·보존 조건은 유지한다([model-routing.md](model-routing.md) §4).
- 원래 프롬프트와 관찰이 어긋나면 **관찰이 실제로 나온 화면**이다. 사용자가 화면 재현·원래 의도 복원 중 어느 쪽을 요청했는지 따른다. 그 선택이 불명확하고 결과가 달라질 때만 묻는다.

---

## 5. 참조의 유지·변경 범위

### 5.1 요청을 기준으로 축을 나눈다

“이 색감으로 다른 장면”이면 색감만, “같은 인물과 의상으로 포즈만”이면 인물·의상을 유지하고 포즈만 바꾼다. 참조 출처나 장수만으로 자동 변주 강도를 정하지 않는다. 사용자가 유지하라고 한 얼굴·상품·장소·카피를 새 디자인으로 바꾸지 않는다.

### 5.2 관찰과 선택을 구분한다

조명·그레이딩·카메라와 피사체·소품·배치를 구분하면 무엇을 가져올지 명료해진다. 이 분류는 입력의 역할을 설명할 뿐, 어느 축을 금지하는 규칙이 아니다. 사진 한 장에서 정확한 렌즈·색온도·HEX를 실측했다고 주장하지 않는다. 정확값이 필요하면 원본 메타데이터·컬러 자산처럼 확인 가능한 근거를 쓴다.

### 5.3 충돌할 때만 우선순위를 정한다

참조의 조명을 유지하며 의상만 바꾸는 요청은 두 조건을 함께 쓴다. 스타일 라벨이 고정 구도·색감과 충돌할 때만 어떤 조건이 우선인지 분명히 한다. 모든 프롬프트에 권한 표나 내부 축 코드를 넣을 필요는 없다.

### 5.4 변주 폭은 선택사항이다

같은 촬영의 다른 순간, 분위기를 유지한 다른 장면, 자유로운 재해석은 가능한 방향의 예다. D1/D2/D3 번호나 기본 강도를 강제하지 않는다. “알아서 새롭게”라면 목적에 맞게 결정하고, “그대로”나 특정 축 변경이면 그 범위를 지킨다. 여러 참조를 섞었다는 사실만으로 독창성이나 권리 문제가 해결됐다고 판단하지 않는다.

### 5.5 이름·카피·보존 조건

요청된 작가·브랜드·장소·스타일 이름을 일률적으로 삭제하지 않는다. 필요한 시각 특성을 덧붙이면 의미가 명확해지지만, 이름을 일반 묘사로 바꾸며 요청을 잃어서는 안 된다. 실제 상위 정책이나 실행 서비스 제한이 적용되면 그 경계를 따르고 필요한 조정을 설명한다. 내부 작성 규칙을 법률 판단이나 서비스 정책으로 포장하지 않는다.

### 5.6 비교 기준

참조와 결과를 비교할 때는 **요청한 유지 축이 유지됐는지, 변경 축이 바뀌었는지, 원치 않은 변화가 생겼는지**를 본다. 고정된 축 개수로 PASS·복제·합법을 판정하지 않는다. 이전 뷰티 시리즈의 소표본 관찰에서 만든 점수 경계는 일반 합격 기준으로 사용하지 않는다.

### 5.7 검증 범위

프롬프트만 작성했다면 지시의 완결성과 충돌 여부까지만 확인한다. 실제 결과 품질은 생성 결과를 보고 판단한다. 비교 생성·제3자 검토·외부 공개는 요청이나 승인된 작업 범위가 있을 때 수행한다.

---

## 6. 컴파일 타깃 — 관찰 하나가 셋을 먹인다

관찰 레코드는 하나이고, `feeds` 엣지도 하나이고, 컴파일만 타깃 수만큼 생긴다([prompt-graph.md](../prompt-graph.md) §5). 타깃마다 레코드를 다시 만들지 않는다.

**타깃 기본값:** 실행 경로·모델 선택이 필요한 요청에서만 [model-routing.md](model-routing.md) §4의 표면별 기본값을 따른다. 대상 미정의 문안만 필요하면 모델을 고르지 않는다. 직접 API의 GPT Image 2.5 선택과 S1의 기존 engine enum을 구분한다. **Higgsfield는 모델 id로 지정되어 들어온다**(`soul_2`·`nano_banana_pro`·`seedream_v5_pro` 등) — 그 지정 자체가 라우팅 결정이므로 다시 라우팅하지 않는다.

### 6.1 타깃 대조표

| 축 | **GPT Image** (표면별 모델 선택) | **Higgsfield 모델 id** | **Midjourney** |
|---|---|---|---|
| 표면 | S1 스키마 / S2 직접 API / S3 | S2 (`gpt_image_2` 포함) | S3 |
| 길이 | [surfaces.md](surfaces.md) §0-1·§0-2의 실제 상한과 간결성 기준 | 같은 정본의 플랫폼·채널·계약 판정 | 같은 정본의 Midjourney 간결성 기준 |
| 배제·보존 제약 | 네이티브 자연어 / 명시 compiled Tier를 구분 | 별도 필드 없음. 필요한 자연어 제약은 명시 | `--no` 인라인과 Edit 지시를 목적에 맞게 사용 |
| 비율 | S1은 스키마 enum, S2는 실제 모델 정의 | `aspect_ratios` 또는 모델별 대체 필드. 빈 배열만으로 모든 비율 제어를 포기하지 않는다 | `--ar 정수:정수` (소수점 불가) |
| 레퍼런스 전달 | S2는 실제 호출 도구의 미디어 롤 / S1은 계약 필드 | 모델 지원과 실제 호출 도구의 role enum을 함께 확인(§6.4) | 슬롯이 갈린다(§6.5) |
| 조용한 실패 | — | `aspect_ratio` 근사 치환·파라미터 기본값 주입 | 모더레이션이 `--no` 값을 단어 단위로 읽음 |

길이 상한의 정본은 [surfaces.md](surfaces.md) §0-1이다 — 전달 채널·타깃 엔진·기계 계약 세 층 중 가장 좁은 것이 구속한다. 타깃 엔진 층의 값은 위 표다. 구체 배선은 [adapters.md](../adapters.md)·런타임 소관이다. 붙여넣기 UI 표면의 2000자는 S3에만 적용되는 계약이 아니라 현행 메신저 배선의 합성값이다(§3).

### 6.2 산출 형식 — 언제나 한 블록

복사 한 번으로 끝나야 한다. **네거티브도 그 블록 안에 있다.** 다중 컷일 때만 컷당 1블록으로 나눈다(리프 하나 = 블록 하나).

배제·보존 조건은 [surface-contracts.md](surface-contracts.md) §4의 실제 입력 형식을 따른다. MPW 컴파일 형식의 `Negative:` 섹션은 [compiler.md](compiler.md) §2 소관이다.

### 6.3 GPT Image

- 내용은 신호 밀도로 다듬고 실제 채널·엔진·기계 계약 상한도 확인한다. 관찰 레코드를 쓴다고 분량이 저절로 통제된다고 가정하지 않는다.
- S1 기계 핸드오프로 나가면 `ar`·`size`·`quality`는 **스키마 enum이 전부**이고 `python3 contracts/validate.py`를 실제로 통과해야 한다.
- GPT Image 2.5의 직접 API 설정은 [surface-contracts.md](surface-contracts.md) §4.3, 참조·누적 편집은 §3.2를 따른다. Higgsfield `gpt_image_2`는 아래 §6.4의 별도 래퍼이며 2.5 선택자로 바꾸지 않는다.

### 6.4 Higgsfield 모델 id

- **현재 모델 상세 조회 도구의 지원 정보와 실제 호출 도구 스키마를 함께 확인한다.** `parameters` / `aspect_ratios` / `medias.roles`는 모델 지원 정보이며 전송 필드의 enum은 호출 도구가 정한다.
- **백엔드 롤을 그대로 전송하지 않는다.** 현행 `generate_image`·`estimate_image_cost`는 canonical role `image`·`start_image`·`end_image`·`video`·`audio`·`ref_element`를 받으며 서버가 모델별 키로 매핑한다(2026-09-05 도구 스키마 확인). `image_references` 같은 카탈로그 키를 호출 role에 복사하지 않는다.
- **비율·파라미터가 보정될 수 있으므로 읽기 전용 `estimate_image_cost`로 `adjustments`를 확인한다.** 현행 `generate_image`는 항상 작업을 제출하므로 `get_cost:true`를 읽기 전용 사전 점검으로 쓰지 않는다(2026-09-05 도구 스키마 확인).
- **입력별 역할·핵심 보존 조건은 짧게 명시한다.** 보이는 외형 전체는 반복하지 않으며, 원본을 가리키는 보존 지시를 경쟁 권한으로 오판하지 않는다.
- 정체성 보존은 선택한 모델과 호출 도구가 현재 지원하는 참조·Element·기존 ID로 처리한다. 이전 로스터의 배타성이나 훈련 경로를 현재 도구의 기능으로 가정하지 않는다.
- 모델 후보·로스터 스냅샷·신선도 규칙은 [model-routing.md](model-routing.md) 소관이다.

### 6.5 Midjourney

- 길이 판정은 [surfaces.md](surfaces.md) §0-1·§0-2를 따른다. 관찰 레코드 전체를 옮기지 말고 요청에 필요한 시각 조건과 다이얼이 허용한 정체 축만 남긴다.
- **`--no`는 단일 명사만 쉼표로 나열한다.** 모더레이션이 `--no`의 모든 단어를 독립적으로 읽으므로 다어절 구는 오탐을 만든다(`--no modern clothing` → "no clothing"). (공식 문서 확인)
- **레퍼런스 슬롯이 무엇을 가져오는지가 다르다.** Image Prompt = 새 장면의 내용·구도·색 참고 / V8 Edit Model = 참조 생성·변경 지시 / `--sref` = 감도 / 무드보드 = `--p`. 슬롯 선택은 [../midjourney-identity.md](../midjourney-identity.md)가 정한다.
- **무드보드에서는 `--sw`가 조용히 무시된다**(비호환). 강도 다이얼은 `--stylize`다. (공식 문서 확인)
- 일반 Image Prompt에는 **완성 장면**을 쓴다. Edit Model에는 입력 역할·변경점·보존 조건을 쓴다. 두 입력 모드의 지시법을 서로 강제하지 않는다.
- 문법·파라미터의 정본은 [../midjourney-identity.md](../midjourney-identity.md) 소관이다. 이 파일은 from-image 레인이 쓰는 레버만 다룬다.

### 6.6 `feeds` 인터페이스 — 관찰 → 컴파일

| 항목 | 내용 |
|---|---|
| 넘어가는 것 | 축별 값 + 상태 + confidence. `intent`/`signature` 태그(§4.1) |
| 넘어가지 않는 것 | 원본 파일 경로·파일명, `ref-NN` 라벨, 원시 비전 payload, `artifact` 태그가 붙은 값, `unreadable`/`not_visible`/`not_applicable` 값 |
| 받았는지 판정하는 법 | 산출 블록의 각 문장이 레코드의 **어느 축에서 왔는지 역추적 가능**하다. 역추적되지 않는 문장은 관찰이 아니라 창작이다 |

---

## 7. 출력 게이트 체크리스트

### 7.1 관찰 단계

- [ ] 첨부 전부에 대해 레코드가 존재한다. 원시 payload가 문자열 처리기에 들어가지 않았다
- [ ] 필요한 관찰 값에 상태 5종 중 하나 + confidence + 픽셀 근거가 붙어 있다
- [ ] `unreadable`/`not_visible`을 `observed`로 승격한 값이 0개다
- [ ] 감도 축이 관찰 가능한 결과로 적혀 있고, 실측하지 않은 수치를 만들지 않았다
- [ ] 의상이 하드락이면 의상 분석 계약으로 위임했고, 스키마를 여기서 다시 쓰지 않았다

### 7.2 생성물 재컴파일일 때만

- [ ] 구분이 필요한 값은 의도·관찰·결함을 혼동하지 않게 기록했다
- [ ] 비의도적 결함을 장면 요구로 옮기지 않았고 요청된 초현실·비대칭은 보존했다
- [ ] `signature`가 같은 모델 재실행이면 빠졌고, 다른 모델 이식이면 결과 어휘로 재서술됐다
- [ ] I8의 판독 상태와 의도한 카피를 구분했고, 사용자 지정 카피·원본 글자 보존 요구를 유지했다
- [ ] 원래 프롬프트가 제공됐다면 의도 확인에 사용했다. 없으면 관찰과 추론을 구분했고 결과를 가르는 미결 사항만 질문했다

### 7.3 참조 기반 작성

- [ ] 각 참조에서 유지할 축과 바꿀 축이 요청과 일치한다
- [ ] 정확 카피·인물·상품·장소 등 지정 조건을 임의로 삭제·변주하지 않았다
- [ ] 관찰과 창작 선택을 구분하고, 확인하지 않은 수치를 측정값처럼 쓰지 않았다
- [ ] 입력 참조 표현이 실제 전달된 이미지와 대응한다

### 7.4 산출 형식

- [ ] 블록이 하나다. 네거티브가 그 안에 있다. 다중 컷이면 컷당 1블록이다
- [ ] 네거티브 형태가 엔진의 3분기 중 맞는 갈래다
- [ ] 길이가 **세 층 중 가장 좁은 실제 상한** 안이다. 측정 여부와 단위는 surfaces.md §0-1의 조건을 따랐다
- [ ] 파라미터로 실제 전달한 축이 산문에 중복되지 않았다
- [ ] Higgsfield면 롤·비율이 그 모델의 런타임 정의 안이고, 판형이 계약이면 `adjustments`를 확인했다
- [ ] 산출의 각 문장이 레코드의 어느 축에서 왔는지 역추적된다

### 7.5 생성 후

취향 변주의 품질 판정이다. 편집 결과는 지정한 변경·보존·출력 계약으로 검수한다.

- [ ] §5.6의 요청별 유지·변경 기준으로 실제 결과를 비교했다
- [ ] 확인하지 않은 품질·권리·동일성은 검증됐다고 보고하지 않았다

---

## 8. 근거·신선도 — 정본은 surface-evidence.md §7

이 파일이 쓰는 외부 사실(Higgsfield 런타임 프로브 결과, `negative_prompt` 부재, gpt-image 길이 상한, Midjourney 문법·단어 대역, 복제 회피 판례)의 **근거와 확인일은 전부 [surface-evidence.md](surface-evidence.md) §7 재검증 표로 이관됐다. 여기서 다시 스탬프하지 않는다.**

Midjourney 문법·파라미터의 **규칙 서술** 정본은 [../midjourney-identity.md](../midjourney-identity.md)다 — §5 `--no`, §6 레퍼런스 슬롯·`--sref`, §7 무드보드. 이 파일은 from-image 레인이 쓰는 레버만 다룬다.

Higgsfield 항목은 플랫폼이 모델을 추가·제거하면 즉시 낡는다. 신선도 규칙은 [model-routing.md](model-routing.md) §5를 따른다.

### 8.1 [미확인] — 단정하지 말 것

이 레인이 단정하지 않는 항목의 **자격 표시([미확인])와 근거 상태는 [surface-evidence.md](surface-evidence.md) §7 표가 정본이다.** Midjourney V8 계열 미확인 항목도, Higgsfield 롤의 의미적 동작 차이도, `max` 미선언 모델의 레퍼런스 장수 상한도 전부 §7의 해당 행을 본다 — **여기서 목록·개수를 복제하지 않는다.** 각 항목이 왜 [미확인]인지(값 없음 / 문서 간 충돌 / 커뮤니티 단일 출처)와 규칙 서술의 소유 파일은 그 행이 직접 적는다.

§5의 작성·비교 기준은 MPW의 작업 설계이며 외부 모델 성능이나 법적 기준을 주장하지 않는다.
